# create table reprint_type
    name 
# add reprint_type_id to table reprint

# chia thêm page reprint cùng chức năng cho mỗi reprint_type
    - mật định sẽ có sẵn reprint_type name shirt
    - khi thêm 1 reprint_type sẽ hiện thêm menu
